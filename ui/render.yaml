services:
  - type: web
    name: access-request-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run stories_app.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PORT
        value: "8501" 